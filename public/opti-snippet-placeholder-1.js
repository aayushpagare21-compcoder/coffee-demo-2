/*
 * Placeholder for SCRIPT_SRC_1 in components/opti-snippet.tsx.
 *
 * It exists so the <script async src> tag in the head is a real tag making a
 * real request: you can watch its load order in the network panel before the
 * real CDN URL is pasted in. Replace SCRIPT_SRC_1 with the real URL and this
 * file becomes unused.
 */
window.__optiSnippetPlaceholder1 = { loadedAt: Date.now() };
console.debug("[opti-demo-store] placeholder async script 1 executed");
